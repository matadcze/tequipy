services:
  backend:
    command: uv run uvicorn src.api.app:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ./backend:/app
      - backend_venv:/app/.venv
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  backend_venv:
